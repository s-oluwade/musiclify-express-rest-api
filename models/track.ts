export interface ITrack {
    name: string;
    album_id: number;
    artist_id: number;
    spotify_id: string;
    spotify_artist_id: string; 
    spotify_album_id: string;
    external_url: string;
    preview_url: string;
}